{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal local-only permissions for the FilePilot desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
